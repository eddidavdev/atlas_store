<h3>Administrator</h3>
<%= render 'admin/nav_links'%>
<table>
  <thead>
    <tr>
      <th>Logo</th>
      <th>Name</th>
      <th>Owner</th>
      <th></th>
    </tr>
  </thead>
  <tbody>
    <% @brands.each do |brand| %>
      <tr>
        <td><%= link_to image_tag(brand.image_as_thumbnail 50), brand.image if brand.image.attached? %></td>
        <td><%= link_to brand.name.truncate(15), admin_brand_path(brand) %></td>
        <td><%= brand.user.email %></td>
        <td>
        </td>
      </tr>
    <% end %>
  </tbody>
</table>
